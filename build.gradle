plugins {
    id 'com.github.johnrengelman.shadow' version '6.1.0'
    id 'com.github.gmazzo.buildconfig' version '3.0.1'
    id 'java'
}

group = bot_group
version = bot_version

buildConfig {
    className('EzriqueInfo')
    packageName('xyz.deftu.ezrique')

    buildConfigField('String', 'VERSION', "\"${bot_version}\"")
}

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url = 'https://s01.oss.sonatype.org/content/repositories/releases/'
    }

    maven {
        name = 'Dv8tion'
        url = 'https://m2.dv8tion.net/releases/'
    }
    maven {
        name = 'JitPack'
        url = 'https://jitpack.io/'
    }
}

configurations {
    shade
    implementation.extendsFrom(shade)
}

dependencies {
    shade('net.dv8tion:JDA:5.0.0-alpha.1')
    shade('org.mongodb:mongodb-driver-sync:4.4.0')

    shade('xyz.deftu.deftils:Deftils:1.1.0')
    shade('org.apache.commons:commons-lang3:3.12.0')
    shade('commons-io:commons-io:2.11.0')
    shade('commons-validator:commons-validator:1.7')

    shade('com.github.ben-manes.caffeine:caffeine:2.9.2')

    shade('org.apache.logging.log4j:log4j-api:2.14.1')
    shade('org.apache.logging.log4j:log4j-core:2.14.1')
    shade('org.apache.logging.log4j:log4j-slf4j18-impl:2.14.1')

    shade('org.discordbots:DBL-Java-Library:2.0.1')
    shade('com.sparkjava:spark-core:2.9.3')
}

jar {
    manifest.attributes(
            'Main-Class': 'xyz.deftu.ezrique.Bootstrap'
    )
}

shadowJar {
    configurations = [project.configurations.shade]
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    archiveClassifier.set('')
}

jar.finalizedBy(shadowJar)